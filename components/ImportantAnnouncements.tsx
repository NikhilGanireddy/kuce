const ImportantAnnouncements = () => {
    return <main className={`container flex justify-center items-center w-full py-24 gap-12`}>
        <h1 className={` text-2xl font-medium`}> <span className={`text-red-600`}>Important </span> Announcements</h1>
        <div>

        </div>
    </main>
}

export default ImportantAnnouncements
